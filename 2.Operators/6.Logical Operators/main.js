//AND Logic (&&) - only true if both operand are true
console.log(true && true);

//OR Logic (||) -will be true if one of the operand is true
console.log(false || true);

//NOT Logic (!) - flip the boolean value of the operand
console.log(!false);
